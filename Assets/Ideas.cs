using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ideas : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
// in game randomly switch between elements
// can go through the same element as you
// the obstalces are all the same but might be one lane or all 3 lanes
// example obstacle would be having fire obstacle on top and water obstacle on bottom forcing you to choose one
// element powerups, ex. water power up can heal, fire powerup can burn obstacle